{
  "name": "@wraith/stellar-identity-sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for Stellar Identity Core contract integration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "pnpm run build && node --test dist/index.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^15.1.0"
  }
}
